const rawSchema = 'maos_master_raw';

declare({
    name: "ads",
    schema: rawSchema,
});

declare({
    name: "adsets",
    schema: rawSchema,
});

declare({
    name: "campaigns",
    schema: rawSchema,
});

declare({
    name: "coupons",
    schema: rawSchema,
});

declare({
    name: "creatives",
    schema: rawSchema,
});

declare({
    name: "statistics_app",
    schema: rawSchema,
});

declare({
    name: "statistics_avg_cart",
    schema: rawSchema,
});

declare({
    name: "statistics_pre_click",
    schema: rawSchema,
});

declare({
    name: "statistics_revenue",
    schema: rawSchema,
});

declare({
    name: "statistics_sales",
    schema: rawSchema,
});

declare({
    name: "statistics_user_agg",
    schema: rawSchema,
});

declare({
    name: "user_agg_adset_month",
    schema: rawSchema,
});

declare({
    name: "user_agg_adset_week",
    schema: rawSchema,
});

declare({
    name: "user_agg_adset_year",
    schema: rawSchema,
});

declare({
    name: "user_agg_campaign_month",
    schema: rawSchema,
});

declare({
    name: "user_agg_campaign_week",
    schema: rawSchema,
});

declare({
    name: "user_agg_campaign_year",
    schema: rawSchema,
});
